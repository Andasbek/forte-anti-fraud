# 🌐 Frontend — Anti-Fraud UI (Vite + React + TS + Tailwind)

## 1. Назначение

Фронтенд — это web-интерфейс к антифрод-сервису:

* позволяет вручную “собирать” транзакции и отправлять их на скоринг,
* показывает вероятность фрода и уровень риска,
* визуализирует риск (воронка) и историю проверок в рамках сессии,
* даёт сценарные пресеты для быстрых демо на жюри.

---

## 2. Технологический стек

* **Vite** — сборка и dev-сервер
* **React + TypeScript**
* **Tailwind CSS v4** — стилизация интерфейса
* API-вызовы через `fetch` (см. `src/services/api.ts`)

---

## 3. Структура фронтенда

Каталог `frontend/`:

```text
frontend/
├─ index.html
├─ package.json
├─ tsconfig.json
├─ vite.config.ts
├─ tailwind.config.js
├─ src/
│  ├─ main.tsx          # входная точка React-приложения
│  ├─ App.tsx           # основной экран: форма, результат, история, воронка
│  ├─ index.css         # глобальные стили + Tailwind directives
│  ├─ services/
│  │  └─ api.ts         # функции для вызова backend API
│  └─ components/
│     └─ (опционально, если выделены) RiskLegend, ...
```

### Ключевые файлы

* **`src/App.tsx`**

  * Левая панель:

    * форма ввода параметров транзакции и поведенческих фич,
    * блок “Статистика сессии” (кол-во проверок, средний риск, доля high),
    * блок сценарных пресетов (нормальный клиент, потенциальный фрод, новый девайс+крупная сумма, ночные мелкие переводы).
  * Правая панель:

    * карточка результата (вероятность фрода, риск-лейбл),
    * визуализация “воронки риска” (gauge-like bar),
    * короткая интерпретация решений,
    * таблица истории проверок.

* **`src/services/api.ts`**

  * Определяет типы запросов/ответов:

    * `ScoreTransactionRequest`
    * `ScoreTransactionResponse`
  * Функция:

    * `scoreTransaction(payload)` — отправляет `POST /api/v1/score_transaction` на backend.
  * Учитывает переменные окружения:

    * `VITE_API_BASE_URL`
    * `VITE_API_PREFIX`
    * `VITE_API_KEY` (если используется API-токен).

* **`tailwind.config.js` + `src/index.css`**

  * Включают Tailwind v4 (через CLI) и базовые слои (`@import "tailwindcss";` и т.п.).
  * В конфиге указаны файлы, где Tailwind ищет классы (`content`).

---

## 4. Установка и команды

### 4.1. Установка зависимостей

```bash
cd frontend
npm install
```

### 4.2. Основные команды

```bash
# Dev-режим (с HMR)
npm run dev

# Production-билд
npm run build

# Превью production-сборки (после build)
npm run preview

# Линтер (если нужен)
npm run lint
```

По умолчанию:

* dev-сервер Vite стартует на `http://localhost:5173`
* ожидается, что backend доступен на `http://localhost:8000`

---

## 5. Переменные окружения (frontend/.env)

Фронт может работать и без `.env` (по умолчанию бьёт в `http://localhost:8000/api/v1`), но для гибкости можно создать `frontend/.env`:

```env
# Базовый URL backend API
VITE_API_BASE_URL=http://localhost:8000

# Префикс API (совпадает с API_V1_PREFIX на backend)
VITE_API_PREFIX=/api/v1

# Если на backend включён API_TOKEN — сюда кладём ключ
# и он автоматически пойдёт в заголовок X-API-Key
VITE_API_KEY=
```

> ⚠️ После изменения `.env` нужно перезапустить `npm run dev`,
> потому что Vite читает env-переменные при старте.

---

## 6. Взаимодействие с backend

В `src/services/api.ts` реализовано:

```ts
const API_BASE_URL =
  import.meta.env.VITE_API_BASE_URL || "http://localhost:8000";
const API_PREFIX =
  import.meta.env.VITE_API_PREFIX || "/api/v1";

const API_KEY = import.meta.env.VITE_API_KEY;

const SCORE_URL = `${API_BASE_URL}${API_PREFIX}/score_transaction`;

export async function scoreTransaction(
  payload: ScoreTransactionRequest
): Promise<ScoreTransactionResponse> {
  const headers: HeadersInit = {
    "Content-Type": "application/json",
  };

  if (API_KEY) {
    headers["X-API-Key"] = API_KEY;
  }

  const res = await fetch(SCORE_URL, {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!res.ok) {
    throw new Error(`API error: ${res.status}`);
  }

  return (await res.json()) as ScoreTransactionResponse;
}
```

Таким образом:

* Если `API_TOKEN` на backend пустой → `VITE_API_KEY` можно не задавать.
* Если `API_TOKEN` задан (например, `secret123`) → в `frontend/.env` нужно прописать:

  ```env
  VITE_API_KEY=secret123
  ```

---

## 7. Сценарии демо (как показывать жюри)

### 7.1. Подготовка

1. Запустить **backend** (с Docker или без, главное, чтобы он отвечал на `http://localhost:8000`).

2. В отдельном терминале запустить **frontend**:

   ```bash
   cd frontend
   npm run dev
   ```

3. Открыть в браузере:
   `http://localhost:5173`

---

### 7.2. Общий вид интерфейса

Экран разделён на две части:

* **Слева**:

  * заголовок “Forte Anti-Fraud Scoring”,
  * блок **статистика сессии** (счётчик проверок, средний риск, доля high),
  * форма с полями:

    * `Amount` — сумма перевода,
    * `OS versions last 30d`,
    * `Phone models last 30d`,
    * `Login sessions last 7d`,
    * `Login sessions last 30d`,
    * (ниже) дополнительные фичи: частота логинов, интервалы и т.п.
  * набор **кнопок-пресетов**:

    * “Пример нормального клиента”
    * “Пример потенциального фрода”
    * “Новый девайс + крупная сумма”
    * “Серия мелких переводов ночью”

* **Справа**:

  * область результата:

    * крупный процент риска фрода,
    * бейдж уровня риска (`low` / `medium` / `high`),
    * подпись с версией модели,
  * **воронка риска** (горизонтальный бар: зелёный → жёлтый → красный + маркер),
  * текстовая интерпретация решения (что значит low/medium/high),
  * **таблица истории** всех запросов в текущей сессии.

---

### 7.3. Сценарии демо шаг за шагом

#### Сценарий 1 — “Нормальный клиент”

1. Нажать кнопку **“Пример нормального клиента”**.
2. Убедиться, что сумма и поведение “спокойные” (небольшое количество логинов, стабильность).
3. Нажать **“Оценить риск”**.
4. Показать:

   * небольшой `%` вероятности,
   * зелёный бейдж `Low risk`,
   * позицию маркера в левом / зелёном сегменте воронки,
   * строку в истории с низким риском.

Месседж: модель не шумит, “нормальные” транзакции не трогает.

---

#### Сценарий 2 — “Подозрительный кейс (потенциальный фрод)”

1. Нажать **“Пример потенциального фрода”**.
2. Обратить внимание на:

   * большую сумму (`amount`),
   * нестабильные / взрывные параметры логинов.
3. Нажать **“Оценить риск”**.
4. Показать:

   * высокий `%`,
   * бейдж `High risk`,
   * маркер в красной зоне воронки,
   * строку в истории с high-risk.

Месседж: модель чувствительна к аномальному поведению и крупным суммам.

---

#### Сценарий 3 — “Новый девайс + крупная сумма”

1. Нажать **“Новый девайс + крупная сумма”**.
2. Пояснить: “клиент недавно сменил устройство и делает большой перевод”.
3. Нажать **“Оценить риск”**.
4. Обсудить:

   * риск может быть medium/high,
   * это кейс для soft-проверки: push/SMS, доп. аутентификация.

---

#### Сценарий 4 — “Серия мелких переводов ночью”

1. Нажать **“Серия мелких переводов ночью”**.
2. Пояснить: много логинов и операций за короткий период, даже если суммы маленькие.
3. Нажать **“Оценить риск”**.
4. Показать, что поведенческие признаки (частота и взрывность логинов) тоже поднимают риск, даже без огромной суммы.

---

### 7.4. Что ещё показать жюри

* **История проверок**:

  * показать, что каждое нажатие “Оценить риск” добавляет запись с:

    * временем,
    * суммой,
    * ключевыми фичами,
    * риском и уровнем.
  * подчеркнуть, что backend **дополнительно** логирует эти же события в SQLite (для будущих дашбордов и анализа дрейфа).

* **Статистика сессии**:

  * “За этот демо-сеанс мы проверили N транзакций, средний риск такой-то, доля high — такая-то”.
  * Это задел под будущие realtime-дэшборды.

---

## 8. Типичные проблемы и быстрые фиксы

* **Ошибка 401 Unauthorized на фронте:**

  * Причина: на backend установлен `API_TOKEN`, а `VITE_API_KEY` не передаётся.
  * Решения:

    * либо обнулить `API_TOKEN` в `.env` backend (для dev),
    * либо прописать `VITE_API_KEY=<тот же токен>` в `frontend/.env` и перезапустить `npm run dev`.

* **CORS-ошибка в браузере:**

  * Проверить, что backend разрешает `http://localhost:5173` в CORS (в `main.py` это уже настроено).
  * Убедиться, что backend реально слушает на `localhost:8000`.
